apiVersion: 1
providers:
  - name: EvolvixOS
    orgId: 1
    folder: EvolvixOS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
